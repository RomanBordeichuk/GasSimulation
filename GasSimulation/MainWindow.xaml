<Window x:Class="GasSimulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GasSimulation.UIRendering"
        mc:Ignorable="d"
        Title="GasSimulation" Height="450" Width="800" Background="Black"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown"
        MouseWheel="Window_Scroll" MouseMove="Window_MouseMove">
    <Grid x:Name="MainContainer">
        <Grid.RenderTransform>
            <MatrixTransform x:Name="ScaleMatrix" />
        </Grid.RenderTransform>
        <local:SimulationField 
            x:Name="SimulationField" />
        <Canvas x:Name="DebugCanvas" />
    </Grid>
</Window>
